---
name: layout-architecture-boards
description: "Plan, compose, review, and preflight evidence-led architectural presentation boards, drawing sheets, architecture posters, portfolios, and long-format review boards. Use when Codex needs to 排版、组版、审查或改进建筑展板/图板/正图，建立网格和视觉层级，安排总图/平面/剖面/轴测/照片/分析图，处理图片比例和裁切、同尺度 A/B 对照、标题正文图注层级、长竖板阅读节奏，或验证 InDesign/PPT/PDF 输出；也适用于 A0/A1、自定义大幅面、单张长板和方案级预排版。"
---

# 建筑图板排版

把建筑图板视为一条有证据、有主次、有阅读距离的论证，而不是若干图片卡片的拼接。先锁定内容与证据，再建立版面骨架；先通过无文字和同尺度检查，再做字体、颜色与输出优化。

## 先判定任务阶段

把任务归入以下阶段，并只做到获准的阶段：

1. **内容盘点**：核对来源、图纸、照片、图解、未知项和交付要求。
2. **方案级预排版**：输出线框、图框比例、阅读顺序或审阅 PDF；不得称为正式成果。
3. **可编辑正式排版**：编辑 INDD/IDML/PPT/SVG/代码原稿；需用户已确认尺寸、内容取舍和层级。
4. **印刷/提交输出**：导出并预检 PDF；需确认打印方或课程的尺寸、出血、色彩和文件标准。

若尺寸、格式、观看距离或正式交付授权尚未确认，继续完成可逆的盘点和线框，但把相关值标为待确认。不要用旧模板、上一轮尺寸或漂亮的占位图代替确认。

## 读取与工具路由

开始前执行以下读取：

- 读取工作区的 `AGENTS.md`、教师/竞赛任务书、当前交付状态和原始证据规则。
- 涉及本用户的第五次汇报或相似的长竖板时，读取 [references/project-profile-long-board.md](references/project-profile-long-board.md)。不要把其中的项目特例自动套到普通 A0/A1 图板。
- 需要解释排版依据或重新制定规则时，读取 [references/research-basis.md](references/research-basis.md)。
- 涉及 INDD/PDF、印刷或最终导出时，读取 [references/qa-and-output.md](references/qa-and-output.md)。
- 使用与文件格式匹配的专门 Skill 完成实际编辑：PDF 用 `pdf`，PPT/PPTX 用 `presentations`，DOCX 用 `documents`。本 Skill 负责建筑图板的内容组织与验收逻辑。

如需建立工作简报，复制 [assets/board-layout-brief-template.md](assets/board-layout-brief-template.md) 到项目工作目录后填写；不要直接修改 Skill 内的模板。

## 核心工作流

### 1. 建立只读清单

先盘点，不先拖图框：

- 确认板面数量、物理尺寸、方向、观看方式、提交格式和截止状态。
- 列出每项素材的 `编号 / 路径 / 类型 / 像素或图纸尺寸 / 宽高比 / 证据等级 / 预期角色 / 是否同尺度 / 是否已批准`。
- 区分 `项目事实 / 通用参考 / 作者分析 / 设计方案 / 未知或待核验`。
- 标记必须成组出现的关系，例如平面—剖面、现状—方案、总图—局部、照片—抽象图、S1 基线—S2/S3 对比。
- 标记重复视角、重复证据、过低分辨率、错误版本、旧占位图和禁止进入正式版面的素材。
- 保留原始文件与上一版输出；在工作副本中修改，不覆盖回退版本。

把资料不足视为排版输入，而不是待“美化”掉的缺口。不得用网络图、AI 图或通用节点填补项目事实。

### 2. 写出一句话论点和阅读路径

先写一句话回答“这张板要让人理解什么”。再建立三层阅读：

- **远看**：识别题目、案例分区、主图和整体节奏。
- **中距**：看懂模块顺序、图组主次、现状/分析/方案关系。
- **近看**：读取图注、来源、尺寸、比例尺、未知项和细节。

按论证关系排序，不按文件类型排序。建筑分析模块优先采用：

`真实来源 → 人工提炼或关键图纸 → 抽象原理 → 同尺度比较 → 未知项`

每个模块指定一个主判断和一个主图。其余图片只承担证据、步骤、细节或对照角色。删去不增加新信息的重复视角。

### 3. 先画骨架，再放素材

用数字化网格建立版面：

- 锁定页面尺寸、外边距、列数、沟槽、基线、分区带和页脚安全区。
- 对长竖板建立明确的纵向预算；给每个模块记录起点、终点和最小高度。
- 让图框、图注和说明块共享真实图组边界，不让文字跨越无关图片。
- 用对齐、间距和底色形成分组；不要把每个内容都做成同权重的网页卡片。
- 给主图、证据带、比较图和文字区分配不同权重；不要让所有图一样大。
- 先输出灰度线框或低保真预览，做一次无文字检查，再进入细排。

相关的平面、剖面、立面或 A/B 图必须对齐；需要准确互相参照时保持同尺度、同方向、同基线。若为了层级改变尺度，明确标注比例并避免产生“仍然同尺度”的错觉。

### 4. 按图像角色选择适配方式

始终保持源图宽高比，不拉伸：

- **技术图、关系图、剖面、完整模型图**：优先 `contain`，保留完整关系；通过裁掉源图无效空边、调整图框或重排网格提高利用率。
- **摄影证据**：仅在不删掉关键主体和证据关系时使用 `cover`；逐张指定主体安全区，不盲用中心裁切。
- **同尺度比较**：使用相同图框、相同视角、相同裁切逻辑和共享基线；不得分别“放到看起来差不多大”。
- **过程序列**：保持顺序、编号、箭头和节奏一致；不得为填满版面改写事实顺序。

若照片被迫成为不可读的扁条，按以下顺序解决：

1. 删除重复证据；
2. 改变列数、改成上下两行或扩大主图；
3. 压缩无关文字；
4. 在方案级阶段提出增加板高或拆板候选；
5. 最后才考虑更换素材。

不得通过拉伸、生成式补图或加入无依据新图来“消化空白”。空白过多时，优先放大关键图组、改善裁切或重新分配网格；密度过高时，优先删减重复内容。

### 5. 建立语义化字体系统

先定义角色，再设置字号：

`板名 → 案例/章节 → 模块标题 → 正文/分析 → 图注/来源 → 身份/页脚`

执行以下规则：

- 使用一个字体家族；确有必要时最多两个，并保持图纸内部文字一致。
- 用有限的字号、字重、颜色和位置表达层级；避免每个模块单独发明样式。
- 以最终物理尺寸和预期观看距离确定字号，并进行 100% 样张或局部试印；不要仅凭屏幕缩放判断。
- 不要为了塞入更多内容单独缩小正文或图注。先删字、改行宽、重排空间。
- 需要整体增大时，对所有语义角色使用同一个比例生成少量候选，再逐一检查换行、图组边界和页脚安全区。
- 把正文放在所属图组下方或明确的相邻栏中；不要夹在图片之间形成错误归属。

字号比例候选是诊断工具，不是自动答案。选中版本后保留未选候选作为回退，不连续覆盖同一个文件。

### 6. 控制颜色、线宽与留白

- 使用浅中性背景和有限的强调色；让颜色承担案例、状态或关系编码，不承担装饰。
- 让事实、分析、方案和未知项在颜色/线型/标签上可区分，但不要只靠颜色传达。
- 保持稳定的线宽层级；在最终尺寸检查细线、虚线、比例尺和小字是否仍可见。
- 同级留白保持一致；不同级留白形成节奏。
- 同时排除两类问题：内容挤满导致“窒息”，以及外边、模块内或窄栏中出现无意的大空洞。

### 7. 分层迭代，不做无穷微调

每一轮只解决一种问题，并生成可比较版本：

1. 内容与证据；
2. 网格与阅读顺序；
3. 图框比例与裁切；
4. 图文边界与文字换行；
5. 字号与颜色；
6. 输出与印刷 QA。

每轮记录“问题、修改、保持不变的约束、输出文件、人工待确认项”。版本名使用稳定的序号或明确目的；不要创建含糊的“最终版2”。

### 8. 执行四种视觉检查

在交付前全部执行：

1. **缩略图检查**：只看主次、节奏、案例分区和空白分布。
2. **无文字检查**：遮住标题和正文，确认仍能看懂对象、步骤、现状/方案差异。
3. **100% 或实尺寸检查**：查看正文、图注、线宽、比例尺、来源和裁切主体。
4. **局部放大检查**：查看接缝、标注、像素、伪字符、重叠、裁切和 AI 图错误。

另做证据检查、同尺度检查、重复视角检查、未知项检查和角色标签检查。将“画面好看”与“论证成立”分别判定。

### 9. 执行文件与输出预检

对 PDF 使用 bundled audit：

先解析当前 Skill 目录的绝对路径，再运行：

```bash
python /absolute/path/to/layout-architecture-boards/scripts/audit_board_pdf.py \
  path/to/board.pdf \
  --expected-pages 1 \
  --expected-size-mm 600x1800 \
  --required-text "板面标题" \
  --forbid-text "占位" \
  --render-dpi 18 \
  --render-dir path/to/qa-render
```

把参数改成真实任务要求。脚本检查页面、尺寸、结构、字体嵌入、文本、嵌图，并生成渲染图；它不能替代人工视觉检查。若系统缺少 Poppler 或 qpdf，按警告补做等价检查。

在 InDesign 中同时检查 Links 和 Preflight；核对缺失/修改链接、有效分辨率、缺失字体、溢出文字、页面范围、出血、色彩和打印方要求。不要把“成功导出”当作“输出正确”。

## 硬性失败条件

发现以下任一项，不得把文件称为最终或可提交：

- 页面数量、物理尺寸、方向或页面范围错误；
- 图片被拉伸、关键证据被裁掉、同尺度比较失真；
- 字体缺失、正文溢出、对象重叠、黑块、伪字符或不可读小字；
- 图纸关系错位、比例尺/北箭头/来源缺失且任务需要；
- 项目事实、通用参考、作者方案和未知项混写；
- 网络图或 AI 图被当作项目证据；
- 未获正式排版授权却将方案级预览标为正式成果；
- 只验证进程、导出命令或 PDF 可打开，没有检查整页和局部渲染。

## 交付格式

交付时直接报告：

- 当前阶段与是否经过人工确认；
- 输出文件、页数、物理尺寸和回退版本；
- 采用的阅读结构、主图层级、裁切策略和同尺度关系；
- 已完成的视觉检查与技术预检；
- 仍待用户、教师、设计者或打印方决定的事项；
- 任何保留的证据边界和不得误读的内容。

不要用“整体不错”代替具体证据。明确区分“排版结构通过”“文件技术检查通过”“内容证据通过”和“正式提交已获批准”。
